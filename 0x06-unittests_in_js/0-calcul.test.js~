const assert = require('assert');
const calculateNumber = require("./0-calcul.js")

describe('Addition Function', ()=>{
    it('it should the sum of two positive numbers', () => {
	const result = calculateNumber(2, 3);
	assert.strictEqual(result, 5);
    });

});
